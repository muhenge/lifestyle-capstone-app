<nav>
  <div class="nav-class">
    <a href="#" class="brand-logo">Logo</a>
    <ul id="nav-mobile" class="right hide-on-med-and-down">
      <li>  <%= link_to "Add a new article", new_article_path, class: 'login' %>
      </li>
      <li><%= link_to "Logout", destroy_session_path, data:{method: :delete}, class: 'login' %></li>
    </ul>
  </div>
</nav>
    <div class="row">
      <% @articles.each do |article| %>

        <div class="col s12 m6 l3">
          <div class="card small" style="background-image: url('<%= rails_blob_url(article.image) if article.image.attached? %>');">
            <div class="card-content white-text">
              <span class="card-title"><%= link_to article.title.capitalize, article_path(article) %></span>
            </div>
            <div class="card-action">
              <%= link_to "Delete", article_path(article), method: :delete, class:"waves-effect waves-light btn red" %>
            </div>
          </div>
        </div>

      <% end %>
    </div>


<table class="striped centered responsive-table">
      <thead>
      <tr>
        <th>

          <%= link_to "Add", new_article_path, class:"btn-floating btn-large waves-effect waves-light btn" %>

        </th>

      </tr>
      </thead>
      <tbody>
      <% @articles.each do |article| %>
        <tr>
          <td>
            <div class="row">

              <div class="col s12 m12 l3"><h6></div>
              <div class="col s12 m12 l3">
                <%= link_to "Read it", article_path(article), class:"waves-effect waves-light btn"%>
                <%= link_to "Delete", article_path(article), method: :delete, class:"waves-effect waves-light btn red" %>
              </div>
            </div>
          </td>
        </tr>
      <% end %>
      </tbody>
    </table>

  </div>

</div>
